<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

	<!-- Check if we already imported them as props. -->
	<Import Project="$(MSBuildThisFileDirectory)MSBuild.LegacyExtensions.Core.props" Condition="'$(ImportedMSBuildLegacyExtensionsCoreProps)' != 'true'" />
	<Import Project="$(MSBuildThisFileDirectory)MSBuild.LegacyExtension.$(_SdkLanguageName).targets" />

</Project>
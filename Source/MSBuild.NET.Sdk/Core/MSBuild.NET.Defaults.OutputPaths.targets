<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<!-- ISSUE: Import the core default output paths from the 'MSBuild.Core.Sdk' only if it's not imported with the 'ProjectCore.targets' -->
	<Import Project="$(MSBuildCoreSdkPath)Core\MSBuild.OutputPaths.targets" Condition="'$(ImportedMSBuildOutputPathsTargets)' != 'true'"/>

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

</Project>
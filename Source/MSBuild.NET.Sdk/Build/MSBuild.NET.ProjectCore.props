<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<ImportProjectDefaults Condition="'$(ImportProjectDefaults)' == ''">true</ImportProjectDefaults>
	</PropertyGroup>

	<ImportGroup Condition="'$(ImportProjectDefaults)' == 'true'">
		<Import Project="$(MSBuildThisFileDirectory)..\Core\MSBuild.NET.Defaults.props" />

		<!-- We need to init _SdkLanguageName, etc. properties. -->
		<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.LanguageInference.props" />
		<Import Project="$(MSBuildThisFileDirectory)..\Core\Languages\MSBuild.$(_SdkLanguageName).props" />

		<Import Project="$(MSBuildThisFileDirectory)..\Core\MSBuild.NET.Defaults.Items.props" />
	</ImportGroup>

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

</Project>
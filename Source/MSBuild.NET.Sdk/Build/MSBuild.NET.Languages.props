<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<ImportLegacyExtensionProps Condition="'$(ImportLegacyExtensionProps)' == ''">true</ImportLegacyExtensionProps>
	</PropertyGroup>

	<!-- We need to init _SdkLanguageName, etc. properties. -->
	<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.LanguageInference.props" />
	<Import Project="$(MSBuildThisFileDirectory)..\Core\Languages\MSBuild.NET.$(_SdkLanguageName).props" />

</Project>
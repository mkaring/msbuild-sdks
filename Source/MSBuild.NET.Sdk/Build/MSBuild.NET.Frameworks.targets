<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<!-- Check if we already imported them as props. -->
	<Import Project="$(MSBuildThisFileDirectory)MSBuild.NET.FrameworkInference.props" Condition="'$(ImportedMSBuildNETInferenceFrameworkProps)' != 'true'" />
	<Import Project="$(MSBuildThisFileDirectory)..\Core\Frameworks\MSBuild.NET.$(_SdkFrameworkName).targets" />

</Project>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

	<!-- We need to init _MSBuildToolsVersion property. -->
	<Import Project="$(MSBuildThisFileDirectory)MSBuild.LegacyExtensions.Core.props" />
	<Import Project="$(MSBuildThisFileDirectory)MSBuild.LegacyExtension.$(_SdkLanguageName).props" />

</Project>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
	</PropertyGroup>

	<!-- TODO: The Project Defaults in the Core Sdk. Remove it once the migration is done -->
	<Import Project="$(MSBuildCoreSdkPath)Build\MSBuild.ProjectDefaults.targets" />
	<!--<Import Project="$(MSBuildThisFileDirectory)..\Legacy\MSBuild.Common.props" />-->

	<PropertyGroup>
		<MicrosoftCommonPropsHasBeenImported>true</MicrosoftCommonPropsHasBeenImported>
	</PropertyGroup>

</Project>